export default function Icon({ height, width }) {
    return (
        <div
            className="bg-[#8D4A52] rounded-lg"
            style={{
                height: `${height}rem`,
                width: `${width}rem`,
            }}
        ></div>
    );
}